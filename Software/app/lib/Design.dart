import 'package:flutter/material.dart';


const TextStyle colorText = TextStyle(color: Colors.white,);
const bgColor = Colors.black;
const accentColor = Colors.redAccent;
const TextStyle accentText = TextStyle(color: accentColor);
const Divider listdivider = Divider(color: Colors.white, height: 1,);

const pendingColor = Colors.blue;
const errorColor = Colors.yellow;

const TextStyle Text_sendcolor = TextStyle(color: accentColor, );
const TextStyle Text_receivecolor = TextStyle(color: Colors.white);
const TextStyle Text_Time_Style = TextStyle(color: Colors.grey, fontSize: 10);
const TextStyle infoText = TextStyle(color: Colors.white, fontSize: 10);

const pendingIcon = Icon(Icons.access_time, color: pendingColor,);
const successIcon = Icon(Icons.check, color: accentColor);
const errorIcon = Icon(Icons.error, color: errorColor);